<div class="container">
  <div class="row">
    <div class="col">
      <% if users.exists? %>
        <% users.each do |user| %>
          <table>
            <thead>
              <tr>
                <th>プロフィール</th>
                <th>　　　　　　　　　　</th>
                <th>フォロー数</th>
                <th>フォロワー数</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                  <td>
                    <%= link_to public_user_path(user.id) do %>
                      <%= image_tag user.get_profile_image(50,50) %>
                    <% end %>
                  </td>
                  <td><%= link_to public_user_path(user.id) do %><%= user.user_nickname %><% end %></td>
                <td><%= user.followings.count %></td>
                <td><%= user.followers.count %></td>
              </tr>
            </tbody>
          </table>
        <% end %>
      <% else %>
        <p>該当するユーザーを見つけられませんでした。</p>
      <% end %>
    </div>
  </div>
</div>