<div class="container">
  <h3 class="m-3"><strong><%= @post.title %></strong></h3>
  <div class="row">
    <div class="col m-3">
      <h2 class="m-3">この投稿を非表示にしますか？</h2>
        <%= form_with model: @post, url: admin_post_path, method: :patch do |f|%>
    　     <%= f.radio_button :is_active, :false %>この投稿を非表示にする
        　<%= f.submit '適用する' %>
        <% end %>
    </div>
  </div>
 </div>

